{
  "name": "databonk-vite-app",
  "version": "0.0.1",
  "private": true,
  "description": "Minimal Vite app exercising databonk — 100K-row pipeline with table preview.",
  "type": "module",
  "scripts": {
    "postinstall": "node scripts/copy-wasm.mjs",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "databonk": "file:../.."
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
